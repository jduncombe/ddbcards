# DnD Beyond Character Card Generator

**N.B This system is dpendent on ddb characters being public.**

# Setup instructions
1. uv venv
2. source .venv/bin/activate
3. uv pip install -r requirements.txt
4. python ddbcards.py -i <character id from ddb>
